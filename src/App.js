import React, { Component } from 'react';
import ReactLeafCarousel from './ReactLeafCarousel'

class App extends Component {
  render() {
    return (
        <div className="container">
          <ReactLeafCarousel />
        </div>
    );
  }
}

export default App;